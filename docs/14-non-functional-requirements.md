# 14. Non-Functional Requirements

* **NFR-01 (Security Practice):** User passwords must be securely hashed using cryptographic salts prior to persistence in the database layer.
* **NFR-02 (UI Responsiveness):** The system interface layout must adapt seamlessly across screen widths ranging from 320px (mobile phones) to 1920px (desktop monitors) using explicit CSS3 media rules.
